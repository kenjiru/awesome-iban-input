{
  "name": "react-es6-bootstrap",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.16"
  },
  "devDependencies": {
    "node-sass": "^4.5.3",
    "rewire": "^2.5.2",
    "sass-loader": "^6.0.6"
  },
  "scripts": {
    "start": "node scripts/customized-config start",
    "build": "GENERATE_SOURCEMAP=false node scripts/customized-config build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
